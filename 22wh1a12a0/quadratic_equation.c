#include<stdio.h>
#include<math.h>
void main()
{
int a,b,c,d;
printf("enter the number a,b,c,d");
scanf("%d%d%d%d",&a,&b,&c,&d);
d=b*b-(4*a*c);
if(d>0)
printf("enter the root1");
scanf("%d%d",&root1);
root1=(-b+sqrt(d))/(2*a);
root2=(-b-sqrt(d))/(2*a);
printf("root1,root2");
printf("roots are real");
{
if(else)
if(d==0)
printf("enter the root2");
scanf("%d%d",root2);
root1=-b/(2*a);
root2=-b/(2*a);
printf("root1,root2");
printf("roots are equal");}
{
if(else)
printf("roots are imaginary");}
return;
}
